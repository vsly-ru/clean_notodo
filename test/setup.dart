// init environment before running each test

import 'package:notodo/core/constants.dart';
import 'package:notodo/di.dart';

void testSetUp() {
  // can be used to work with custom (mocked) repositories under the test environment
  // configureDependencies(Env.test);
}
